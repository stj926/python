
np.atleast_xd:维度改变 支持将输入数据直接视为 x维。这里的 x 可以表示：1，2，3

np.argmax:返回沿轴最大值的索引值

np.expand_dims:用于扩展数组的形状

numpy中的ravel()、flatten()、squeeze()都有将多维数组转换为一维数组的功能，区别:
ravel():如果没有必要，不会产生源数据的副本,源数据将被修改
flatten():返回源数据的副本
squeeze():只能对维数为1的维度降维
