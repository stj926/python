物理结构：顺序存储，链式存储
逻辑结构：集合，线性结构，树形结构，图状结构

原子类型和结构类型





抽象数据类型::

线性表：
顺序表（数组）（线性表顺序存储结构）：内存连续 地址计算方法：loc(ai) = loc(a1) + (i-1) * c ，c为占用的存储单元（字节）
单链表（线性表链式存储结构）：内存不连续 头指针-头结点（可无）-结点-null
静态链表：用于没有指针的语言
循环链表
双向链表

栈（一般用顺序存储结构）：先进后出，后进先出LIFO
队列（一般用链式存储结构）：先进先出FIFO（顺序存储效率低下）
  循环队列防止溢出
字符串：（一般顺序存储结构）



二叉树：
红黑树：O(lgn)
